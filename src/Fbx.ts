export class Fbx{
    constructor(){

    }

    // WriteBinary(FbxDocument document, string path)
    // {
    //     if (path == null)
    //         throw new ArgumentNullException(nameof(path));
    //     using (var stream = new FileStream(path, FileMode.Create))
    //     {
    //         var writer = new FbxBinaryWriter(stream);
    //         writer.Write(document);
    //     }
    // }
}